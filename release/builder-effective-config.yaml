directories:
  output: release
  buildResources: build
appId: com.pig.cleaner
files:
  - filter:
      - dist/renderer/**/*
      - src/main/**/*
      - package.json
mac:
  target:
    - target: dmg
      arch:
        - arm64
        - x64
electronVersion: 43.1.0
